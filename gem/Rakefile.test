# frozen_string_literal: true

require 'bundler/setup'

require 'teneo/format_library'

begin
  require 'rspec/core/rake_task'
  RSpec::Core::RakeTask.new(:spec)
rescue LoadError
end

require 'teneo/format_library/tasks'
